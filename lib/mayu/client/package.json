{
  "name": "mayu-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "esbuild src/*.ts --sourcemap=inline --outdir=dist --watch",
    "build": "esbuild src/*.ts --sourcemap=inline --outdir=dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/serviceworker": "^0.0.48",
    "esbuild": "^0.14.48",
    "prettier": "^2.7.1",
    "typescript": "^4.7.4"
  }
}
