{
  "name": "@mayu-live/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mayu-live/prototype.git"
  },
  "bugs": {
    "url": "https://github.com/mayu-live/prototype/issues"
  },
  "homepage": "https://github.com/mayu-live/prototype#readme",
  "scripts": {
    "dev": "npm run build -- --watch --sourcemap=inline",
    "build": "esbuild src/*.ts --outdir=dist",
    "build:production": "npm run build -- --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/serviceworker": "^0.0.48",
    "esbuild": "^0.14.48",
    "typescript": "^4.7.4"
  }
}
