[dev]
  secret_key = "dev"

  [dev.server]
    scheme = "https"
    host = "localhost"
    port = 9292
    hot_swap = true

[prod]
  [prod.server]
    scheme = "https"
    host = "0.0.0.0"
    port = 3000
    hot_swap = true
    processes = 4
    event_source_retry_ms = 1000
