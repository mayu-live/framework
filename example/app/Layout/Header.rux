Tagline = import("./Tagline")
Clock = import("./Clock")

render do
  <header class={styles.header}>
    <div class={styles.inner}>
      <h1 class={styles.title}>mayu live</h1>
      <Tagline class={styles.tagline} />
    </div>
  </header>
end
