def render
  classname = [
    styles[:select],
    props[:class],
  ].compact.join(" ")

  h.div class: styles.group do
    h.select(class: classname, placeholder: props[:label], **props) do
      children.each { h << _1 }
    end

    h.label props[:label], class: styles[:label]
  end
end
