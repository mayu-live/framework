:css
  .maxWidth {
    width: min(100% - 2rem, var(--page-max-width));
    margin-inline: auto;
  }

:ruby
  Mayu::VDOM.h(
    props[:tag] || :div,
    *children,
    class: styles[:maxWidth, props[:class]]
  )
