:ruby
  # class scope

:ruby
  level = props[:level].to_i.clamp(1, 6)
  tag = :"h#{level}"
  classname = styles[tag, styles[:class]]

  Mayu::VDOM::h(
    tag,
    *children,
    **props.except(:level).merge(class: classname),
  )
