def render
  classnames =
    [styles.maxWidth, props[:class]]
    .compact
    .join(" ")

  tag = props[:tag] || :div

  Mayu::VDOM::Descriptor.new(tag.to_sym, {class: classnames}, props[:children])
end
