:ruby
  DynamicMarkdown = import("/components/DynamicMarkdown")

%li{class: $role}
  %strong= $role
  %DynamicMarkdown= $text

:css
  li {
    display: flex;
    gap: .5em;
    padding: .5em;
    margin: 0;
  }

  strong {
    &::after {
      content: ": ";
    }
  }

  .user {
    background: #0063;
  }

  .assistant {
    background: #0603;
  }

  DynamicMarkdown {
    & > :first-child {
      margin-top: 0;
    }

    & > :last-child {
      margin-bottom: 0;
    }
  }
