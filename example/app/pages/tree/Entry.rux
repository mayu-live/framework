Directory = require("./Directory")
FileEntry = require("./FileEntry")

def render
  props => {path:}

  if File.directory?(path)
    <Directory path={path} entry={self.class} initial-open={props[:initial_open]} />
  else
    <FileEntry path={path} />
  end
end
